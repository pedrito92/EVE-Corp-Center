CREATE TABLE IF NOT EXISTS `ecc_pages` (
  `ID_page` int(11) NOT NULL auto_increment,
  `name_page` varchar(200) NOT NULL,
  `slug_page` varchar(200) NOT NULL,
  PRIMARY KEY  (`ID_page`)
) ENGINE=MyISAM  DEFAULT CHARSET=utf8 AUTO_INCREMENT=1;

‡